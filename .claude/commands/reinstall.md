Reinstall workbench using pipx.

Run: `pipx install --force .` from the project root `/Users/kyle.waldner/src/workbench`.

This rebuilds and reinstalls the `workbench` CLI tool so that any code changes take effect.
